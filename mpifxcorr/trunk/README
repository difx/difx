Example installation guide.  Assumes mpi c++ compiler is 
<openmpiprefix>/bin/mpicxx 

svn co []
aclocal
autoconf
autoheader
automake -a
./configure --prefix=${DIFX_PREFIX}  CXX=${OPENMPI_PREFIX}/bin/mpicxx
make
# (su root?)
make install
